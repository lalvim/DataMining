# Modelos de notebooks

O arquivo `modelo_notebook.ipynb` contém a estrutura mínima obrigatória definida em `contexto/diretrizes_formatacao.md`. Copie o modelo para a pasta da unidade e substitua todos os campos entre colchetes antes da revisão.
